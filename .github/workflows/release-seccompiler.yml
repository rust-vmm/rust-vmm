name: Publish seccompiler to crates.io

on:
  push:
    tags: ['seccompiler-v*']

jobs:
  publish:
    runs-on: ubuntu-latest
    permissions:
      id-token: write
    steps:
    - uses: actions/checkout@v4
    - uses: rust-lang/crates-io-auth-action@v1
      id: auth
    - run: cargo publish --package seccompiler
      env:
        CARGO_REGISTRY_TOKEN: ${{ steps.auth.outputs.token }}
